const About = () => {
    return <div> THIS IS THE ABOUT PAGE</div>;
  };
  
  export default About;
//as router and routes is built in NEXT
//to access the about page just do localhost:3000/about